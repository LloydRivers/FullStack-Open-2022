/*

Create an interface called Employee with the following properties:

id: number
name: string
age: number

Create a function called createEmployee that takes in parameters for id, name, and age, and returns an object of type Employee.

Define the return type of the createEmployee function.
*/

// Your code goes here...
interface Employee {
  id: number;
  name: string;
  age: number;
}
